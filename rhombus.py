# Finding Area of Rhombus if Base and Height are given

base = int(input('Enter the base of rhombus'))
height = int(input('Enter the height of rhombus'))
area = base * height
print('Area is', area) 